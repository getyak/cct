interface:
  display_name: "Cct"
  short_description: "Repo-specific patterns and workflows for cct"
  default_prompt: "Use the cct repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true